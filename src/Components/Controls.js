import React from 'react';

export default function Controls(props) {
    return (
        <section id="controls" className="section is-small">
            <button id="start-btn" className="button">
                Start Search
            </button>
            <button id="next-btn" className="button">
                Next Step
            </button>
        </section>
    );
}
